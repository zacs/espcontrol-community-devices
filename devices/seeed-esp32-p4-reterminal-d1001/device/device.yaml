# =============================================================================
# DEVICE - Seeed reTerminal D1001 hardware definition
# =============================================================================
# Pin assignments, peripherals, and platform config for the 8" touch panel.
# Includes the ESP32-P4 main SoC, ESP32-C6 hosted WiFi co-processor, MIPI
# DSI display (JD9365), GSL3670 capacitive touchscreen, an XL9535/PCA9535 I2C
# GPIO expander (display power/reset/backlight-enable + power latch), and
# backlight PWM. Pin map and power-up sequence follow Seeed's reTerminal D1001
# BSP (github.com/Seeed-Studio/reTerminal-D1001).
# =============================================================================

substitutions:
  espcontrol_component_url: "https://github.com/jtenniswood/espcontrol"
  espcontrol_component_ref: "v2.10.0"
  mdi_font_file: "https://cdn.jsdelivr.net/npm/@mdi/font@7.4.47/fonts/materialdesignicons-webfont.ttf"

# ---------------------------------------------------------------------------
# ESP32-P4 platform (16MB flash, ESP-IDF framework)
# ---------------------------------------------------------------------------
esp32:
  variant: esp32p4
  flash_size: 16MB
  cpu_frequency: 360MHZ
  engineering_sample: true
  framework:
    type: esp-idf
    # stick to recommended version, currently 5.5.4, 55.03.39 with tool-esptoolpy 5.3.0. No issues
    #version: 5.5.2
    # pioarduino 55.03.38-1 pulls tool-esptoolpy 5.2.0, which can install
    # incompletely in ESPHome containers and then fail while creating .bin files.
    #platform_version: 55.03.37
    advanced:
      enable_idf_experimental_features: true
      execute_from_psram: true
      disable_vfs_support_termios: false
      disable_vfs_support_select: false
      disable_vfs_support_dir: false
    sdkconfig_options:
      CONFIG_SDMMC_HOST_DEFAULT: "y"
      CONFIG_COMPILER_OPTIMIZATION_SIZE: "y"
      # HTTPS firmware checks need more stack on ESP32-P4; the default main
      # task stack can trip the stack protector during mbedTLS setup.
      CONFIG_ESP_MAIN_TASK_STACK_SIZE: "8192"
      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: "y"
      # Native OTA and display startup can block loopTask briefly on this
      # panel. Use the ESP-IDF maximum so those longer operations can finish.
      CONFIG_ESP_TASK_WDT_TIMEOUT_S: "60"
      # The P4 firmware image is large enough that yielding during every
      # flash erase can make native OTA preparation exceed ESPHome's uploader
      # timeout before the progress bar starts.
      CONFIG_SPI_FLASH_YIELD_DURING_ERASE: "n"
      # ESP-Hosted preallocates a DMA transport buffer pool before ESPHome
      # starts; disabling it (our old fix here) avoided the "HS_MP: mempool
      # create failed: no mem" OTA-rollback boot failure but left inbound HA
      # API/web requests dependent on dynamic buffer allocation, which can
      # silently starve and never recover (jtenniswood/espcontrol#1099).
      # Upstream's real fix keeps the pool but relocates it to PSRAM instead
      # (esp32_hosted: use_psram: true below), which fixes both. Let
      # WiFi/LWIP buffers use PSRAM too.
      CONFIG_ESP_HOSTED_DFLT_TASK_FROM_SPIRAM: "y"
      CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP: "y"
      CONFIG_FREERTOS_THREAD_LOCAL_STORAGE_POINTERS: "1"
      CONFIG_LOG_DEFAULT_LEVEL: "2"

# ---------------------------------------------------------------------------
# External components
# ---------------------------------------------------------------------------
external_components:
  - source:
      type: git
      url: ${espcontrol_component_url}
      ref: ${espcontrol_component_ref}
      path: components
    components: [espcontrol, web_server_idf, artwork_image]

espcontrol:
  id: espcontrol_app
  panel_config:
    device_profile: ${device_slug}
    storage: card_images
    button_order: button_order
    button_on_color: button_on_color
    buttons:
      - config: button_1_config
        subpage_chunks: [subpage_1_config, subpage_1_config_ext, subpage_1_config_ext_2, subpage_1_config_ext_3, subpage_1_config_ext_4, subpage_1_config_ext_5, subpage_1_config_ext_6, subpage_1_config_ext_7]
      - config: button_2_config
        subpage_chunks: [subpage_2_config, subpage_2_config_ext, subpage_2_config_ext_2, subpage_2_config_ext_3, subpage_2_config_ext_4, subpage_2_config_ext_5, subpage_2_config_ext_6, subpage_2_config_ext_7]
      - config: button_3_config
        subpage_chunks: [subpage_3_config, subpage_3_config_ext, subpage_3_config_ext_2, subpage_3_config_ext_3, subpage_3_config_ext_4, subpage_3_config_ext_5, subpage_3_config_ext_6, subpage_3_config_ext_7]
      - config: button_4_config
        subpage_chunks: [subpage_4_config, subpage_4_config_ext, subpage_4_config_ext_2, subpage_4_config_ext_3, subpage_4_config_ext_4, subpage_4_config_ext_5, subpage_4_config_ext_6, subpage_4_config_ext_7]
      - config: button_5_config
        subpage_chunks: [subpage_5_config, subpage_5_config_ext, subpage_5_config_ext_2, subpage_5_config_ext_3, subpage_5_config_ext_4, subpage_5_config_ext_5, subpage_5_config_ext_6, subpage_5_config_ext_7]
      - config: button_6_config
        subpage_chunks: [subpage_6_config, subpage_6_config_ext, subpage_6_config_ext_2, subpage_6_config_ext_3, subpage_6_config_ext_4, subpage_6_config_ext_5, subpage_6_config_ext_6, subpage_6_config_ext_7]
      - config: button_7_config
        subpage_chunks: [subpage_7_config, subpage_7_config_ext, subpage_7_config_ext_2, subpage_7_config_ext_3, subpage_7_config_ext_4, subpage_7_config_ext_5, subpage_7_config_ext_6, subpage_7_config_ext_7]
      - config: button_8_config
        subpage_chunks: [subpage_8_config, subpage_8_config_ext, subpage_8_config_ext_2, subpage_8_config_ext_3, subpage_8_config_ext_4, subpage_8_config_ext_5, subpage_8_config_ext_6, subpage_8_config_ext_7]
      - config: button_9_config
        subpage_chunks: [subpage_9_config, subpage_9_config_ext, subpage_9_config_ext_2, subpage_9_config_ext_3, subpage_9_config_ext_4, subpage_9_config_ext_5, subpage_9_config_ext_6, subpage_9_config_ext_7]
      - config: button_10_config
        subpage_chunks: [subpage_10_config, subpage_10_config_ext, subpage_10_config_ext_2, subpage_10_config_ext_3, subpage_10_config_ext_4, subpage_10_config_ext_5, subpage_10_config_ext_6, subpage_10_config_ext_7]
      - config: button_11_config
        subpage_chunks: [subpage_11_config, subpage_11_config_ext, subpage_11_config_ext_2, subpage_11_config_ext_3, subpage_11_config_ext_4, subpage_11_config_ext_5, subpage_11_config_ext_6, subpage_11_config_ext_7]
      - config: button_12_config
        subpage_chunks: [subpage_12_config, subpage_12_config_ext, subpage_12_config_ext_2, subpage_12_config_ext_3, subpage_12_config_ext_4, subpage_12_config_ext_5, subpage_12_config_ext_6, subpage_12_config_ext_7]
      - config: button_13_config
        subpage_chunks: [subpage_13_config, subpage_13_config_ext, subpage_13_config_ext_2, subpage_13_config_ext_3, subpage_13_config_ext_4, subpage_13_config_ext_5, subpage_13_config_ext_6, subpage_13_config_ext_7]
      - config: button_14_config
        subpage_chunks: [subpage_14_config, subpage_14_config_ext, subpage_14_config_ext_2, subpage_14_config_ext_3, subpage_14_config_ext_4, subpage_14_config_ext_5, subpage_14_config_ext_6, subpage_14_config_ext_7]
      - config: button_15_config
        subpage_chunks: [subpage_15_config, subpage_15_config_ext, subpage_15_config_ext_2, subpage_15_config_ext_3, subpage_15_config_ext_4, subpage_15_config_ext_5, subpage_15_config_ext_6, subpage_15_config_ext_7]
      - config: button_16_config
        subpage_chunks: [subpage_16_config, subpage_16_config_ext, subpage_16_config_ext_2, subpage_16_config_ext_3, subpage_16_config_ext_4, subpage_16_config_ext_5, subpage_16_config_ext_6, subpage_16_config_ext_7]
      - config: button_17_config
        subpage_chunks: [subpage_17_config, subpage_17_config_ext, subpage_17_config_ext_2, subpage_17_config_ext_3, subpage_17_config_ext_4, subpage_17_config_ext_5, subpage_17_config_ext_6, subpage_17_config_ext_7]
      - config: button_18_config
        subpage_chunks: [subpage_18_config, subpage_18_config_ext, subpage_18_config_ext_2, subpage_18_config_ext_3, subpage_18_config_ext_4, subpage_18_config_ext_5, subpage_18_config_ext_6, subpage_18_config_ext_7]
      - config: button_19_config
        subpage_chunks: [subpage_19_config, subpage_19_config_ext, subpage_19_config_ext_2, subpage_19_config_ext_3, subpage_19_config_ext_4, subpage_19_config_ext_5, subpage_19_config_ext_6, subpage_19_config_ext_7]
      - config: button_20_config
        subpage_chunks: [subpage_20_config, subpage_20_config_ext, subpage_20_config_ext_2, subpage_20_config_ext_3, subpage_20_config_ext_4, subpage_20_config_ext_5, subpage_20_config_ext_6, subpage_20_config_ext_7]

# ---------------------------------------------------------------------------
# ESPHome core (device name from substitutions)
# ---------------------------------------------------------------------------
esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  platformio_options:
    build_flags:
      - "-Wno-deprecated-declarations"
      - "-DESPCONTROL_DISABLE_TODO=1"
      # Force PlatformIO to rebuild cached ESPHome objects after the 2026.5
      # core/scheduler/watchdog changes; stale 2026.4 objects can boot-loop.
      - "-DESPCONTROL_ESPHOME_2026_5_REBUILD=1"
      # Keep LVGL active while the backlight is off so the GSL3670 touch
      # controller can still deliver the wake tap from display-off screensaver.
      - "-DESPCONTROL_KEEP_LVGL_ACTIVE_ON_DISPLAY_OFF=1"
      # Build marker for the initial reTerminal D1001 bring-up.
      - "-DESPCONTROL_RETERMINAL_D1001_BOOTFIX_20260607=1"
    build_src_flags:
      - "-Wno-literal-suffix"
  on_boot:
    - priority: 260
      then:
        - if:
            condition:
              lambda: |-
                #ifdef USE_ESP32_CRASH_HANDLER
                return esphome::esp32::crash_handler_has_data();
                #else
                return false;
                #endif
            then:
              - lambda: |-
                  #ifdef USE_ESP32_CRASH_HANDLER
                  esphome::esp32::crash_handler_clear();
                  #endif
                  id(reterminal_d1001_safe_mode).mark_successful();
                  global_preferences->sync();
                  ESP_LOGW("reterminal_d1001", "Cleared saved crash report before API logging; rebooting once");
              - delay: 500ms
              - lambda: |-
                  App.safe_reboot();
    - priority: -100
      then:
        - lambda: 'id(screen_rotation_ready) = true;'
        - script.execute: apply_screen_rotation

# ---------------------------------------------------------------------------
# ESP32-C6 co-processor (provides WiFi/BLE via SDIO)
# ---------------------------------------------------------------------------
esp32_hosted:
  variant: esp32c6
  # Keep the preallocated transport receive buffers for reliable inbound
  # connections, but place them in PSRAM to protect scarce internal memory.
  use_psram: true
  reset_pin: GPIO13
  cmd_pin: GPIO6
  clk_pin: GPIO11
  d0_pin: GPIO7
  d1_pin: GPIO8
  d2_pin: GPIO9
  d3_pin: GPIO10
  active_high: true
  sdio_frequency: 20MHz

# ---------------------------------------------------------------------------
# PSRAM (video frame buffer memory)
# ---------------------------------------------------------------------------
psram:
  mode: hex
  speed: 200MHz

# ---------------------------------------------------------------------------
# OTA updates (ESPHome native + HTTP-based)
# ---------------------------------------------------------------------------
ota:
  - platform: esphome
    on_begin:
      then:
        - script.execute: show_update_writing
        - script.wait: show_update_writing
    on_end:
      then:
        - globals.set:
            id: firmware_update_complete_pending
            value: 'true'
        - lambda: 'global_preferences->sync();'
    on_error:
      then:
        - script.execute: show_update_failed
  - platform: http_request
    on_begin:
      then:
        - script.execute: show_update_writing
        - script.wait: show_update_writing
    on_end:
      then:
        - globals.set:
            id: firmware_update_complete_pending
            value: 'true'
        - lambda: 'global_preferences->sync();'
    on_error:
      then:
        - script.execute: show_update_failed

safe_mode:
  id: reterminal_d1001_safe_mode

# ---------------------------------------------------------------------------
# Shared infrastructure (API, logging, web server, restart button)
# ---------------------------------------------------------------------------
packages:
  core_infra: !include ../../../common/device/core_infra.yaml

# ---------------------------------------------------------------------------
# Power management (LDO channel for DSI PHY)
# ---------------------------------------------------------------------------
esp_ldo:
  - channel: 3
    id: dsi_phy_enable
    voltage: 2.5V
    adjustable: True

# ---------------------------------------------------------------------------
# Backlight (LEDC PWM output + monochromatic light entity)
# ---------------------------------------------------------------------------
output:
  - platform: ledc
    pin: GPIO14
    id: gpio_backlight_pwm
  # Bezel RGB status LED. Pins follow Seeed's reTerminal D1001 BSP
  # (R=GPIO22, G=GPIO36, B=GPIO23). The LED is common-anode, so each channel
  # is active-low: the BSP writes duty = 100 - percent, which inverted: true
  # reproduces here.
  - platform: ledc
    pin: GPIO22
    id: status_led_r
    inverted: true
    frequency: 1000Hz
  - platform: ledc
    pin: GPIO36
    id: status_led_g
    inverted: true
    frequency: 1000Hz
  - platform: ledc
    pin: GPIO23
    id: status_led_b
    inverted: true
    frequency: 1000Hz

light:
  - platform: monochromatic
    output: gpio_backlight_pwm
    name: Display Backlight
    icon: mdi:lightbulb-on
    id: display_backlight
    restore_mode: RESTORE_DEFAULT_ON
    default_transition_length: 0s
    gamma_correct: 1.0
    on_turn_on:
      then:
        - if:
            condition:
              lambda: |-
                auto now = panel_time_or_fallback(id(panel_time).now(), id(homeassistant_time).now());
                return (id(espcontrol_app).display().target_source_is(
                            espcontrol::DisplayRequestSource::SCREEN_SCHEDULE) &&
                        id(espcontrol_app).display().target_mode_is(
                            espcontrol::DisplayMode::DISPLAY_OFF)) ||
                       screen_schedule_waiting_for_time(
                         id(screen_schedule_trigger).state,
                         id(schedule_enabled).state,
                         now.is_valid());
            then:
              - script.execute: backlight_force_display_off
            else:
              - if:
                  condition:
                    lambda: |-
                      return id(espcontrol_app).display().current_mode_is(
                          espcontrol::DisplayMode::DISPLAY_OFF);
                  then:
                    - script.execute: screensaver_wake
    on_turn_off:
      then:
        - if:
            condition:
              lambda: |-
                return id(espcontrol_app).display().takeover_active(
                           espcontrol::DisplayTakeoverKind::INTERACTIVE) ||
                       id(espcontrol_app).display().takeover_active(
                           espcontrol::DisplayTakeoverKind::CRITICAL);
            then:
              - lambda: 'ESP_LOGD("backlight", "Restoring backlight after OFF event during display takeover");'
              - delay: 10ms
              - script.execute: backlight_apply_brightness
            else:
              - if:
                  condition:
                    lambda: |-
                      return id(espcontrol_app).display().target_mode_is(
                          espcontrol::DisplayMode::ACTIVE);
                  then:
                    - script.execute: screen_schedule_manual_sleep
    on_state:
      then:
        - script.execute: display_backlight_handle_state
  # Bezel RGB status LED on the active-low LEDC outputs above (BSP pin map:
  # R=GPIO22, G=GPIO36, B=GPIO23). Exposed to Home Assistant for
  # automations; EspControl takes no local action on it.
  - platform: rgb
    name: Status LED
    id: status_led
    icon: mdi:led-on
    red: status_led_r
    green: status_led_g
    blue: status_led_b
    restore_mode: RESTORE_DEFAULT_OFF
    default_transition_length: 0s
    effects:
      - pulse:
      - strobe:
          name: Red Flash
          colors:
            - state: true
              brightness: 100%
              red: 100%
              green: 0%
              blue: 0%
              duration: 500ms
            - state: false
              duration: 500ms
      - strobe:
          name: Green Flash
          colors:
            - state: true
              brightness: 100%
              red: 0%
              green: 100%
              blue: 0%
              duration: 500ms
            - state: false
              duration: 500ms
      - strobe:
          name: Blue Flash
          colors:
            - state: true
              brightness: 100%
              red: 0%
              green: 0%
              blue: 100%
              duration: 500ms
            - state: false
              duration: 500ms

# ---------------------------------------------------------------------------
# I2C buses
#   bus_touch    - GSL3670 touch controller (I2C0 on the BSP)
#   bus_expander - XL9535/PCA9535 GPIO expander (I2C1 on the BSP)
# ---------------------------------------------------------------------------
i2c:
  - id: bus_touch
    sda: GPIO37
    scl: GPIO38
    frequency: 400kHz
  - id: bus_expander
    sda: GPIO20
    scl: GPIO21
    frequency: 400kHz

# ---------------------------------------------------------------------------
# GPIO expander (XL9535/PCA9535). Drives the display power/reset/backlight
# enable lines (used by the mipi_dsi model below) and the soft-power latch.
# ---------------------------------------------------------------------------
xl9535:
  - id: xl9535_hub
    i2c_id: bus_expander
    address: 0x20

# ---------------------------------------------------------------------------
# Soft-power latch. The reTerminal D1001 gates its 3.3V rail behind the
# expander's PWR_HOLD line; it must be driven high at boot to keep the board
# powered, matching bsp_power_init() in Seeed's BSP. The BSP addresses it as
# bit 8; ESPHome's xl9535 numbers the second port 10-17, so bit 8 is pin 10.
# ---------------------------------------------------------------------------
switch:
  - platform: gpio
    id: power_hold
    internal: true
    restore_mode: ALWAYS_ON
    pin:
      xl9535: xl9535_hub
      number: 10

# ---------------------------------------------------------------------------
# Physical button (GPIO3). Active-high with an external pull-down, matching
# the reTerminal D1001 BSP (active_level=1, disable_pull=true). Exposed to
# Home Assistant for automations; EspControl takes no local action on it.
# ---------------------------------------------------------------------------
binary_sensor:
  - platform: gpio
    name: Button
    id: physical_button
    icon: mdi:gesture-tap-button
    pin:
      number: GPIO3
      mode:
        input: true
        pulldown: true

# ---------------------------------------------------------------------------
# Touchscreen (GSL3670 capacitive, wakes screensaver on touch). Uses
# ESPHome's native gsl3670 touchscreen platform (merged upstream in
# esphome/esphome#16285) rather than an external component, so there's no
# more dependency on any fork branch surviving. The firmware blob comes from
# the driver's companion repo, esphome-libs/gsl3670-firmware, verified by
# SHA-256 at compile time.
#
# reset_pin is the touch controller's TP_RST, and wiring it is load-bearing
# on this panel (issue #113). The BSP calls it BSP_LCD_TOUCH_RST = 1<<12,
# i.e. expander bit 12; ESPHome's xl9535 numbers the second port 10-17, so
# bit 12 is pin 14 — the same +2 offset as power_hold above. That is exactly
# what the built-in SEEED-RETERMINAL-D1001 model defaults to; an earlier
# comment here read its {xl9535, number: 14} default as a raw GPIO14 and
# dropped it to avoid a collision with the backlight PWM, but the two are
# different pins on different controllers and never collided.
#
# The BSP's explicit reset pulse in bsp_touch_new() is commented out, which
# is why this looked optional. It isn't: bsp_power_init() runs
# esp_io_expander_set_dir(io_expander, 0xffff, IO_EXPANDER_OUTPUT), and
# against the PCA9535's all-ones power-on output register that alone drives
# TP_RST high and releases the controller. Leave the pin unconfigured and it
# stays a high-impedance input after the expander's power-on reset, holding
# the GSL3670 in reset: the bus scan finds nothing on bus_touch and every
# firmware-load write NAKs. A warm reboot hid this because the expander is
# not power-cycled and keeps whatever direction bits the previous firmware
# set, so touch only died on a true cold boot — and setup() logs
# "initialised OK" either way, as it checks no write's return value.
#
# model: SEEED-RETERMINAL-D1001 supplies this panel's touch calibration
# (issue #113). Without it the platform falls back to the CUSTOM model, which
# declares no calibration at all, and setup() then uses the display's native
# 800x1280 as the raw range. The controller actually reports its long axis as
# x over roughly 20-1644 and its short axis as y over 20-872, transposed
# relative to the display, so everything past 800 clamps in normalize_() and
# the long axis collapses onto one value - the "wrongly calibrated" taps in
# the issue. The model's ranges match the raw values measured on the
# reporter's hardware.
#
# Note calibration bounds are applied *after* the swap: add_raw_touch_position_
# swaps the raw pair first, then normalises, so x_max 872 belongs to the
# display's 800px axis rather than to the controller's own x.
#
# The transform block is dropped rather than restated, for two reasons. It
# described the panel in the rotated frame, which is wrong: LVGL rotates touch
# input itself (LVTouchListener's read callback calls rotate_coordinates on the
# point), so the transform must describe the native, unrotated panel and must
# not compensate for the lvgl rotation below. And a partial transform: block
# still takes per-key defaults from the model, so a leftover one would only
# invite drifting back out of sync.
#
# interrupt_pin, reset_pin and firmware duplicate the model's defaults. They
# stay spelled out: reset_pin is what #114 fixed and naming xl9535_hub beats
# relying on the model's auto-resolved hub id, and pinning the firmware blob's
# SHA-256 here keeps the artifact fixed independently of the model.
# ---------------------------------------------------------------------------
touchscreen:
  - platform: gsl3670
    id: my_touchscreen
    model: SEEED-RETERMINAL-D1001
    i2c_id: bus_touch
    interrupt_pin: GPIO16
    reset_pin:
      xl9535: xl9535_hub
      number: 14
    firmware:
      url: "https://github.com/esphome-libs/gsl3670-firmware/releases/download/v1.0.0/seeed-d1001-fw.bin"
      sha256: "2e50501ad83656fb6fa3d92591f9f31add4d442c8e8a79f29f5c4d335bd127a4"
    on_touch:
      - script.execute: cover_art_pause_after_touch
      - script.wait: cover_art_pause_after_touch
      - script.execute: screensaver_wake

# ---------------------------------------------------------------------------
# Display (8" 800x1280 native MIPI DSI panel, JD9365). Display power, reset,
# and backlight-enable are driven through the XL9535 expander, per the
# built-in SEEED-RETERMINAL-D1001 model.
# ---------------------------------------------------------------------------
display:
  - platform: mipi_dsi
    id: my_display
    model: SEEED-RETERMINAL-D1001
    color_order: RGB
    update_interval: never
    auto_clear_enabled: false
    dimensions:
      width: 800
      height: 1280
    enable_pin:
      - xl9535: xl9535_hub
        number: 0
      - xl9535: xl9535_hub
        number: 7
    reset_pin:
      xl9535: xl9535_hub
      number: 2

# ---------------------------------------------------------------------------
# LVGL base (display binding, must be before any page definitions)
# ---------------------------------------------------------------------------
lvgl:
  id: main_lvgl
  displays: my_display
  rotation: 90
  byte_order: little_endian
  touchscreens:
    - touchscreen_id: my_touchscreen
      long_press_time: 3s

script:
  - id: apply_screen_rotation
    then:
      - if:
          condition:
            lambda: 'return id(screen_rotation_select).current_option() == "90";'
          then:
            - lvgl.display.set_rotation: 180
          else:
            - if:
                condition:
                  lambda: 'return id(screen_rotation_select).current_option() == "180";'
                then:
                  - lvgl.display.set_rotation: 270
                else:
                  - if:
                      condition:
                        lambda: 'return id(screen_rotation_select).current_option() == "270";'
                      then:
                        - lvgl.display.set_rotation: 0
                      else:
                        - lvgl.display.set_rotation: 90
      - script.execute: cover_art_apply_responsive_layout

select:
  - platform: template
    name: "${entity_screen_rotation}"
    internal: true
    disabled_by_default: true
    id: screen_rotation_select
    entity_category: config
    icon: "mdi:screen-rotation"
    optimistic: true
    restore_value: true
    initial_option: "180"
    options:
      - "0"
      - "90"
      - "180"
      - "270"
    on_value:
      then:
        - if:
            condition:
              lambda: 'return id(screen_rotation_ready);'
            then:
              - script.execute: apply_screen_rotation
              - script.execute: refresh_button_grid
